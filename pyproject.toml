[project]
name = "sitemap-to-llmstxt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
    "beautifulsoup4>=4.12.0",
]

[tool.uv.workspace]
members = [
    "backend",
]
